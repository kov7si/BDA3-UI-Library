{
    "testEnvironment": "jsdom",
    "transform": {
        "^.+\\.tsx?$": ["ts-jest", {
            "tsconfig": "./tsconfig.jest.json"
        }]
    },
    "setupFilesAfterEnv": [
        "@testing-library/jest-dom/extend-expect"
    ]
}